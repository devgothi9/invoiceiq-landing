import React from 'react'
import ReactDOM from 'react-dom/client'
import InvoiceIQLanding from '../invoiceiq-landing'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <InvoiceIQLanding />
  </React.StrictMode>,
)
